export enum ComponentStructureType {
  COMPOSITE = 'composite',
  ATOMIC = 'atomic',
}

export enum ComponentCategory {
  PRODUCT = 'product',
  LAYOUT = 'layout',
  MARKETING = 'marketing',
  CONTENT = 'content',
  ACTION = 'action',
}
