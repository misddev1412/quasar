# Build configuration for scripts/build.sh

# Image tag applied to every service image
IMAGE_TAG=latest

# Optional prefix for the image name (e.g., ghcr.io/org/project/)
# Leave blank to auto-generate from the registry + GitHub values below
IMAGE_NAME_PREFIX=

# Optional prefix for the package name (e.g., quasar- to get quasar-frontend)
IMAGE_PACKAGE_PREFIX=

# Target platforms for docker buildx
BUILD_PLATFORMS=linux/amd64,linux/arm64

# Registry + repo info (used when IMAGE_NAME_PREFIX is blank)
DOCKER_REGISTRY=ghcr.io
GITHUB_USERNAME=your-github-username
GITHUB_REPO=your-repo-name
