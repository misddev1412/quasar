# Admin Application Environment Variables
# Copy this file to .env and update the values as needed

# Server Configuration
# Port for the development server (default: 4200)
PORT=4200

# API Configuration (shared with storefront)
# Backend API URL used by the admin app for all API/tRPC calls
# You can include the /api path or just the origin (both are supported)
REACT_APP_API_URL=http://localhost:3000/api
