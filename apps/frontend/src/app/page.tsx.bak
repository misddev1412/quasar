// Root page - will be handled by next-intl middleware
// The middleware will automatically route this to the appropriate locale
export default function RootPage() {
  return (
    <div>
      <h1>Loading...</h1>
      <p>If you see this, the middleware routing is not working properly.</p>
    </div>
  );
}